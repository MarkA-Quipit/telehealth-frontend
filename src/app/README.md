# App Directory

This directory contains application-level configurations and setup.

## Structure

- **layouts/** - Application layout components (MainLayout, AuthLayout, etc.)
- **providers/** - Context providers (AuthProvider, ThemeProvider, etc.)
- **router/** - Route configuration and protected routes setup
